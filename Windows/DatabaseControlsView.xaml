<UserControl x:Class="GTRC_Database_Viewer.Windows.DatabaseControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GTRC_Database_Viewer.Windows"
             mc:Ignorable="d">
    <StackPanel Grid.Row="1" Orientation="Vertical">
        <StackPanel Orientation="Horizontal" Height="30" HorizontalAlignment="Center" Margin="0,5">
            <Button Command="{Binding GenericDatabaseVM.DelCmd}"
                    Style="{StaticResource DelButtonStyle}" Margin="15,0"/>
            <Button Command="{Binding GenericDatabaseVM.ClearCurrentCmd}"
                    Content="Clear" Style="{StaticResource ButtonStyle}" MinWidth="70" Margin="5,0"/>
            <Button Command="{Binding GenericDatabaseVM.UpdateCmd}"
                    Content="Update" Style="{StaticResource ButtonStyle}" MinWidth="70" Margin="5,0"/>
            <Button Command="{Binding GenericDatabaseVM.AddCmd}"
                    Style="{StaticResource AddButtonStyle}" Margin="15,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Height="30" HorizontalAlignment="Center" Margin="0,5">
            <Button Content="SQL" Command="{Binding GenericDatabaseVM.LoadSqlCmd}"
                    Style="{StaticResource ResTextButtonStyle}" Padding="7" Margin="5,0"/>
            <Button Content="SQL" Command="{Binding GenericDatabaseVM.WriteSqlCmd}"
                    Style="{StaticResource SavTextButtonStyle}" Padding="7" Margin="5,0"/>
            <Button Content="Json" Command="{Binding GenericDatabaseVM.LoadJsonCmd}"
                    Style="{StaticResource ResTextButtonStyle}" Padding="7" Margin="5,0"/>
            <Button Content="Json" Command="{Binding GenericDatabaseVM.WriteJsonCmd}"
                    Style="{StaticResource SavTextButtonStyle}" Padding="7" Margin="5,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Height="30" HorizontalAlignment="Center" Margin="0,5">
            <Button Content="SQL" Command="{Binding GenericDatabaseVM.ClearSqlCmd}"
                    Style="{StaticResource BinTextButtonStyle}" Padding="7" Margin="5,0"/>
            <Button Content="Json" Command="{Binding GenericDatabaseVM.ClearJsonCmd}"
                    Style="{StaticResource BinTextButtonStyle}" Padding="7" Margin="5,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Height="30" HorizontalAlignment="Center" Margin="0,5">
            <CheckBox IsChecked="{Binding GenericDatabaseVM.ForceDelete}" Style="{StaticResource SwitchCheckBoxStyle}"/>
            <Label Content="Delete child objects" Style="{StaticResource LabelStyle}" Margin="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>
